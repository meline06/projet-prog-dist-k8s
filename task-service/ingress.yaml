apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: task-service-ingress
spec:
  rules:
  - host: task-service.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: task-service
            port:
              number: 5000